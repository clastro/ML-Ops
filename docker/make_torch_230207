1. pytorch docker 생성
docker run -it -p 12345:8888 -v /:/work/ --gpus all --name my_torch pytorch/pytorch /bin/bash

# -it를 입력하지 않으면 생성 후 바로 종료

2. pytorch 자동으로 attach 
docker attach my_torch #만약 안 되었다면

3. jupyter notebook 등 설치

pip isntall jupyter notebook

4. jupyter notebook 실행
jupyter notebook --allow-root
